這個補丁修復：
- KeyError: 'lanes' in fts_broker_shadow_mutator.py

原因：
- 舊的 state/broker_side_ledger_shadow.json 結構不含 lanes
- mutator 直接 state['lanes'] 導致炸掉

修復內容：
- 自動修復 legacy / malformed shadow ledger payload
- 若缺 lanes/history 會自動補齊
- 若舊 payload 是扁平 order dict，會自動搬到 UNKNOWN lane
